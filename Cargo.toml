[package]
name = "rustdb"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] } #command line arguments
tokio = { version = "1.53.1", features = ["full"] } #network handling
